apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: worker
  labels:
    app: feedparser
    tier: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      tier: worker
    matchExpressions:
      - { key: tier, operator: In, values: [worker] }
  template:
    metadata:
      labels:
        app: feedparser
        tier: worker
    spec:
      containers:
      - name: node-dfg
        image: asia.gcr.io/feedparser-217913/feedparser/node_dfg:latest
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
